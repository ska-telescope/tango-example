{{ $localchart := . }}

{{ range $key, $deviceserver := .Values.deviceServers }}

{{ $context := dict "name" $key "deviceserver" $deviceserver "image" $deviceserver.image "local" $localchart }}

{{ template "tango-util.multidevicesds.tpl" $context }}

{{ end }} # deviceservers