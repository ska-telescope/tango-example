{{ if .Values.tangodb.enabled }}
---

apiVersion: v1
kind: Service
metadata:
  name: tangodb-{{ template "ska-tango-examples.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: tangodb-{{ template "ska-tango-examples.name" . }}
    chart: {{ template "ska-tango-examples.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  ports:
  - name: mysql
    port: 3306
  clusterIP: None
  selector:
    app: tangodb-{{ template "ska-tango-examples.name" . }}

---

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: tangodb-{{ template "ska-tango-examples.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: tangodb-{{ template "ska-tango-examples.name" . }}
    chart: {{ template "ska-tango-examples.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  selector:
    matchLabels:
      app: tangodb-{{ template "ska-tango-examples.name" . }}
  serviceName: tangodb-{{ template "ska-tango-examples.name" . }}
  replicas: 1
  template:
    metadata:
      labels:
        app: tangodb-{{ template "ska-tango-examples.name" . }}
        chart: {{ template "ska-tango-examples.chart" . }}
        release: {{ .Release.Name }}
        heritage: {{ .Release.Service }}
    spec:
      containers:
      - name: tangodb
        image: "{{ .Values.tangodb.image.registry }}/{{ .Values.tangodb.image.image }}:{{ .Values.tangodb.image.tag }}"
        imagePullPolicy: {{ .Values.tangodb.image.pullPolicy }}
        env:
          - name: MYSQL_ROOT_PASSWORD
            value: "{{ .Values.tangodb.db.rootpw }}"
          - name: MYSQL_DATABASE
            value: "{{ .Values.tangodb.db.db }}"
          - name: MYSQL_USER
            value: "{{ .Values.tangodb.db.user }}"
          - name: MYSQL_PASSWORD
            value: "{{ .Values.tangodb.db.password }}"
        ports:
        - name: mysql
          containerPort: 3306
        volumeMounts:
        - name: data
          mountPath: /var/lib/mysql
        resources:
{{ toYaml .Values.tangodb.resources | indent 10 }}
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: tangodb-{{ template "ska-tango-examples.name" . }}
{{- with .Values.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
{{- end }}
{{- with .Values.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
{{- end }}
{{- with .Values.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
{{- end }}

{{ end }}